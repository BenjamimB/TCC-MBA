# Plano de Testes — 6.1: Configurar LangChain.js com Maritaca e Claude como fallback

## Requisitos cobertos
- 4.1: Motor de IA integrado ao fluxo de conversa WhatsApp
- 4.5: Fallback automático quando provedor primário falhar

---

## 1. Funcionalidade
> Roteamento de provider e execução de completion.

Relevante: SIM

### Partições de equivalência identificadas
| ID | Partição | Tipo | Comportamento esperado |
|----|----------|------|------------------------|
| P1 | provider `maritaca-small` com sucesso | válida | usa ChatOpenAI (maritaca baseURL), retorna conteúdo |
| P2 | provider `maritaca` com sucesso | válida | usa ChatOpenAI (maritaca baseURL), retorna conteúdo |
| P3 | provider `claude-haiku` com sucesso | válida | usa ChatAnthropic, retorna conteúdo |
| P4 | provider `claude-sonnet` com sucesso | válida | usa ChatAnthropic, retorna conteúdo |
| P5 | provider primário falha → fallback executado | edge | fallback provider invocado automaticamente |
| P6 | circuit breaker aberto (≥5 falhas em 60s) | edge | AI_CIRCUIT_OPEN retornado sem chamar provider |
| P7 | circuit breaker fecha após 30s | edge | provider volta a ser chamado |
| P8 | mensagens com tool definitions | válida | tools passadas ao provider, toolCalls retornados |

TCP alvo: cobrir P1–P6 (P7 requer controle de clock; P8 cobre tool calling básico).

### Casos de teste
| ID | Descrição | Partição | Tipo Jest |
|----|-----------|----------|-----------|
| TC-F-01 | `complete()` com maritaca-small retorna conteúdo via ChatOpenAI | P1 | unit |
| TC-F-02 | `complete()` com claude-haiku retorna conteúdo via ChatAnthropic | P3 | unit |
| TC-F-03 | `complete()` com provider primário falhando usa fallback | P5 | unit |
| TC-F-04 | `complete()` retorna AI_CIRCUIT_OPEN após 5 falhas consecutivas | P6 | unit |
| TC-F-05 | `complete()` circuit breaker reseta após período de cooldown | P7 | unit |
| TC-F-06 | `complete()` passa tools ao provider quando fornecidas | P8 | unit |
| TC-F-07 | `complete()` retorna toolCalls quando provider responde com tool_call | P8 | unit |

---

## 2. Confiabilidade
> Fallback e circuit breaker garantem disponibilidade.

Relevante: SIM

### Casos cobertos
- TC-F-03: fallback automático quando primário falha.
- TC-F-04/05: circuit breaker previne sobrecarga do provider com falhas consecutivas.

---

## 3. Performance
> Latência do circuit breaker.

Relevante: SIM — verificação do circuit breaker < 1ms (lógica em memória).

Nota: garantido pela implementação, não medido em nível unitário.

---

## 4. Segurança
> API keys nunca expostas em logs ou respostas.

Relevante: SIM

### Casos cobertos
- Implementação: API keys lidas de variáveis de ambiente apenas; não logadas.
- Testes: adapters mockados — keys nunca usadas em testes unitários.

---

## 5. Manutenibilidade
> Separação de camadas, injeção de dependência.

Relevante: SIM

### Rubrica de testabilidade
| Dimensão | Pontuação (0–25) | Observação |
|---|---|---|
| Injeção de dependência | 25 | Factories de model injetáveis para substituição em testes |
| Funções puras | 20 | Lógica de roteamento e circuit breaker são puras com clock injetável |
| Separação de camadas | 25 | Implementa IAIGateway; domínio não conhece LangChain |
| Observabilidade | 25 | Todos os retornos são `Result<AIResponse>` assertáveis |
| **Total** | **95/100** | |

---

## Cobertura de quality characteristics
| Característica | Relevante | Com teste |
|---|---|---|
| Funcionalidade | ✓ | ✓ |
| Confiabilidade | ✓ | ✓ |
| Performance | ✓ | ✗ (garantido pela implementação) |
| Segurança | ✓ | ✓ (estrutural) |
| Manutenibilidade | ✓ | ✓ (estrutural) |
| Usabilidade | N/A | — |

**Cobertura = 3 ÷ 3 × 100% = 100%**

---

## Resultado da execução
| Métrica | Valor |
|---------|-------|
| Total de testes | 12 |
| Passaram | 12 |
| Falharam | 0 |
| Cobertura de linha | 100% |
| Cobertura de branch | 100% |
| pass@1 | ✅ sim |
