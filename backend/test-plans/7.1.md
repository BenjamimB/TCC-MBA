# Plano de Testes — 7.1: Implementar BookingService com regras de negócio de agendamento

## Requisitos cobertos
- 5.1: Apresentar slots disponíveis e realizar reserva após confirmação
- 5.2: Cancelar consulta existente e apresentar novos horários
- 5.3: Confirmar cancelamento e liberar horário
- 5.4: Atualizar agenda do profissional instantaneamente (via SSE EventBus)
- 5.6: Enviar confirmação ao paciente com data, horário e endereço
- 5.7: Rejeitar agendamento no passado (AC 5.7)
- 5.8: Rejeitar cancelamento de consulta passada (AC 5.8)
- 5.10: Antecedência mínima configurável para agendamento (AC 5.10)

---

## 1. Funcionalidade
> Corretude dos comportamentos especificados nos requisitos.

Relevante: SIM

### Partições de equivalência identificadas
| ID | Partição | Tipo | Comportamento esperado |
|----|----------|------|------------------------|
| P1 | `startAt > now` + dados válidos + chave nova | válida | `createAppointment()` → cria e publica evento |
| P2 | `startAt <= now` | inválida | `createAppointment()` → APPOINTMENT_IN_PAST |
| P3 | `startAt < now + minAdvanceHours` | inválida | `createAppointment()` → BELOW_MIN_ADVANCE_NOTICE |
| P4 | `idempotencyKey` já utilizada | edge | `createAppointment()` → retorna existente sem duplicar |
| P5 | Agendamento futuro para cancelar | válida | `cancelAppointment()` → status=cancelled + evento |
| P6 | Cancelar agendamento inexistente | inválida | `cancelAppointment()` → APPOINTMENT_NOT_FOUND |
| P7 | Cancelar agendamento passado | inválida | `cancelAppointment()` → APPOINTMENT_ALREADY_PAST |
| P8 | Confirmar agendamento pendente existente | válida | `confirmAppointment()` → status=confirmed + evento |
| P9 | Confirmar agendamento inexistente | inválida | `confirmAppointment()` → APPOINTMENT_NOT_FOUND |
| P10 | Busca por id existente | válida | `getById()` → retorna agendamento |
| P11 | Busca por id inexistente | inválida | `getById()` → APPOINTMENT_NOT_FOUND |

TCP alvo: cobrir todas as partições acima.

### Casos de teste
| ID | Descrição | Partição | Tipo Jest |
|----|-----------|----------|-----------|
| TC-F-01 | `createAppointment()` cria agendamento com dados válidos | P1 | unit |
| TC-F-02 | `createAppointment()` retorna APPOINTMENT_IN_PAST para `startAt` no passado | P2 | unit |
| TC-F-03 | `createAppointment()` retorna BELOW_MIN_ADVANCE_NOTICE por antecedência insuficiente | P3 | unit |
| TC-F-04 | `createAppointment()` retorna agendamento existente para `idempotencyKey` repetida | P4 | unit |
| TC-F-05 | `createAppointment()` publica evento `appointment_created` após criar | P1 | unit |
| TC-F-06 | `createAppointment()` não publica evento quando `idempotencyKey` já existe | P4 | unit |
| TC-F-07 | `cancelAppointment()` cancela agendamento futuro com sucesso | P5 | unit |
| TC-F-08 | `cancelAppointment()` retorna APPOINTMENT_NOT_FOUND para id inexistente | P6 | unit |
| TC-F-09 | `cancelAppointment()` retorna APPOINTMENT_ALREADY_PAST para agendamento passado | P7 | unit |
| TC-F-10 | `cancelAppointment()` publica evento `appointment_cancelled` após cancelar | P5 | unit |
| TC-F-11 | `confirmAppointment()` confirma agendamento pendente | P8 | unit |
| TC-F-12 | `confirmAppointment()` retorna APPOINTMENT_NOT_FOUND para id inexistente | P9 | unit |
| TC-F-13 | `confirmAppointment()` publica evento `appointment_confirmed` após confirmar | P8 | unit |
| TC-F-14 | `getById()` retorna agendamento existente | P10 | unit |
| TC-F-15 | `getById()` retorna APPOINTMENT_NOT_FOUND para id inexistente | P11 | unit |
| TC-F-16 | `getByDay()` retorna agendamentos do dia | — | unit |
| TC-F-17 | `getByWeek()` retorna agendamentos da semana | — | unit |

---

## 2. Confiabilidade
> Idempotência e tolerância a reprocessamento.

Relevante: SIM

### Casos cobertos
- TC-F-04 / TC-F-06: `idempotencyKey` garante que reprocessamento de mensagem não cria duplicata.

---

## 3. Performance
> Tempo de resposta.

Relevante: NÃO — performance de queries é responsabilidade do adapter Prisma (índices no schema).

---

## 4. Segurança
> Validação de regras de negócio como segurança de dados.

Relevante: SIM

### Casos cobertos
- TC-F-09: rejeita tentativa de cancelar consulta passada (dados imutáveis após ocorrência).

---

## 5. Manutenibilidade
> Separação de camadas, injeção de dependência.

Relevante: SIM

### Rubrica de testabilidade
| Dimensão | Pontuação (0–25) | Observação |
|---|---|---|
| Injeção de dependência | 25 | `IAppointmentRepository` e `ISSEEventBus` injetáveis; clock injetável |
| Funções puras | 25 | Validações de data são puras; clock controlável nos testes |
| Separação de camadas | 25 | Não conhece Prisma nem ioredis; usa apenas interfaces de porta |
| Observabilidade | 25 | Todos os retornos são `Result<T>` assertáveis |
| **Total** | **100/100** | |

---

## Cobertura de quality characteristics
| Característica | Relevante | Com teste |
|---|---|---|
| Funcionalidade | ✓ | ✓ |
| Confiabilidade | ✓ | ✓ |
| Performance | N/A | — |
| Segurança | ✓ | ✓ |
| Manutenibilidade | ✓ | ✓ (estrutural) |
| Usabilidade | N/A | — |

**Cobertura = 3 ÷ 3 × 100% = 100%**

---

## Resultado da execução
| Métrica | Valor |
|---------|-------|
| Total de testes | 17 |
| Passaram | 17 |
| Falharam | 0 |
| Cobertura de linha | — |
| Cobertura de branch | — |
| pass@1 | ✅ sim |
