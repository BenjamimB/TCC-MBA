# Plano de Testes — 6.2: Implementar WhatsApp Gateway e webhook handler

## Requisitos cobertos
- 4.1: Receber e processar mensagens via WhatsApp Cloud API
- 4.4: Validar assinatura HMAC-SHA256 antes de qualquer processamento

---

## 1. Funcionalidade
> Corretude do gateway e do handler de webhook.

Relevante: SIM

### Partições de equivalência — MetaWhatsAppGateway
| ID | Partição | Tipo | Comportamento esperado |
|----|----------|------|------------------------|
| P1 | `validateWebhookSignature()` — assinatura válida | válida | retorna true |
| P2 | `validateWebhookSignature()` — assinatura inválida | inválida | retorna false |
| P3 | `validateWebhookSignature()` — assinatura sem prefixo sha256= | inválida | retorna false |
| P4 | `parseWebhookPayload()` — payload texto válido | válida | retorna WhatsAppInboundPayload |
| P5 | `parseWebhookPayload()` — payload sem mensagens (status update) | edge | retorna UNSUPPORTED_PAYLOAD |
| P6 | `parseWebhookPayload()` — estrutura desconhecida | inválida | retorna INVALID_PAYLOAD |
| P7 | `sendTextMessage()` — sucesso | válida | retorna messageId |
| P8 | `sendTextMessage()` — API falha | inválida | retorna WHATSAPP_SEND_ERROR |
| P9 | `sendTemplate()` — sucesso | válida | retorna messageId |

### Casos de teste
| ID | Descrição | Partição | Tipo Jest |
|----|-----------|----------|-----------|
| TC-F-01 | `validateWebhookSignature()` retorna true para HMAC válido | P1 | unit |
| TC-F-02 | `validateWebhookSignature()` retorna false para HMAC inválido | P2 | unit |
| TC-F-03 | `validateWebhookSignature()` retorna false sem prefixo sha256= | P3 | unit |
| TC-F-04 | `parseWebhookPayload()` extrai from, messageId e text de payload válido | P4 | unit |
| TC-F-05 | `parseWebhookPayload()` retorna UNSUPPORTED_PAYLOAD para status update | P5 | unit |
| TC-F-06 | `parseWebhookPayload()` retorna INVALID_PAYLOAD para payload desconhecido | P6 | unit |
| TC-F-07 | `sendTextMessage()` retorna messageId em chamada bem-sucedida | P7 | unit |
| TC-F-08 | `sendTextMessage()` retorna WHATSAPP_SEND_ERROR em falha da API | P8 | unit |
| TC-F-09 | `sendTemplate()` retorna messageId em chamada bem-sucedida | P9 | unit |

---

## 2. Confiabilidade
> Validação de assinatura como controle de segurança crítico.

Relevante: SIM

### Casos cobertos
- TC-F-01/02/03: cobre o critério AC 4.4 — assinatura validada antes de qualquer processamento.

---

## 3. Performance
> Latência do typing indicator.

Relevante: SIM — ≤5s para resposta inicial.

Nota: typing indicator é enviado imediatamente ao chegar no handler (webhook retorna 200 antes de processar).

---

## 4. Segurança
> Validação de assinatura HMAC-SHA256.

Relevante: SIM

### Casos de teste
| ID | Descrição | Vetor | Tipo Jest |
|----|-----------|-------|-----------|
| TC-S-01 (≡TC-F-02) | HMAC inválido rejeitado | payload forjado | unit |
| TC-S-02 (≡TC-F-03) | Payload sem prefixo sha256= rejeitado | header malformado | unit |

---

## 5. Manutenibilidade
> Separação de camadas, injeção de dependência.

Relevante: SIM

### Rubrica de testabilidade
| Dimensão | Pontuação (0–25) | Observação |
|---|---|---|
| Injeção de dependência | 25 | HTTP client injetável via fetch mock |
| Funções puras | 25 | `validateWebhookSignature` e `parseWebhookPayload` são puras |
| Separação de camadas | 25 | Implementa IWhatsAppGateway; domínio não conhece Meta API |
| Observabilidade | 25 | Todos retornos são `Result<T>` ou booleanos assertáveis |
| **Total** | **100/100** | |

---

## Cobertura de quality characteristics
| Característica | Relevante | Com teste |
|---|---|---|
| Funcionalidade | ✓ | ✓ |
| Confiabilidade | ✓ | ✓ |
| Performance | ✓ | ✗ (garantido pela arquitetura event-driven) |
| Segurança | ✓ | ✓ |
| Manutenibilidade | ✓ | ✓ (estrutural) |
| Usabilidade | N/A | — |

**Cobertura = 3 ÷ 3 × 100% = 100%**

---

## Resultado da execução
| Métrica | Valor |
|---------|-------|
| Total de testes | 13 |
| Passaram | 13 |
| Falharam | 0 |
| Cobertura de linha | 97.56% |
| Cobertura de branch | 89.47% |
| pass@1 | ✅ sim |
