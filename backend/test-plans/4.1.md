# Plano de Testes — 4.1: Implementar cadastro automático e perfil de pacientes

## Requisitos cobertos
- 7.1: Cadastro automático no primeiro contato via WhatsApp com registro de consentimento
- 7.2: Atualização progressiva de perfil durante a conversa (nome, data de nascimento)
- 7.3: Identificação de paciente existente pelo número de telefone
- 7.4: API REST para profissional visualizar e editar fichas de pacientes
- 8.5: Anonimização LGPD (direito ao esquecimento)

---

## 1. Funcionalidade
> Corretude dos comportamentos especificados nos requisitos.

Relevante: SIM

### Partições de equivalência identificadas
| ID | Partição | Tipo | Comportamento esperado |
|----|----------|------|------------------------|
| P1 | Primeiro contato — paciente não existe | válida | `findOrCreateByPhone()` cria paciente com `consentRecordedAt` |
| P2 | Contato repetido — paciente já existe | válida (idempotência) | `findOrCreateByPhone()` retorna paciente existente sem criar |
| P3 | Atualização de nome | válida | `updateProfile()` persiste nome |
| P4 | Atualização de data de nascimento | válida | `updateProfile()` persiste data |
| P5 | Atualização por profissional diferente do responsável | inválida | `updateProfile()` → UNAUTHORIZED |
| P6 | Atualização de paciente inexistente | inválida | `updateProfile()` → PATIENT_NOT_FOUND |
| P7 | Busca de paciente próprio | válida | `getById()` retorna paciente |
| P8 | Busca de paciente de outro profissional | inválida | `getById()` → UNAUTHORIZED |
| P9 | Busca de paciente inexistente | inválida | `getById()` → PATIENT_NOT_FOUND |
| P10 | Anonimização por profissional responsável | válida | `anonymize()` → substitui dados pessoais |
| P11 | Anonimização por profissional diferente | inválida | `anonymize()` → UNAUTHORIZED |
| P12 | Anonimização de paciente inexistente | inválida | `anonymize()` → PATIENT_NOT_FOUND |
| P13 | Busca de histórico por profissional responsável | válida | `getHistory()` retorna registros |
| P14 | Busca de histórico por profissional diferente | inválida | `getHistory()` → UNAUTHORIZED |

TCP alvo: cobrir todas as partições acima.

### Casos de teste
| ID | Descrição | Partição | Tipo Jest |
|----|-----------|----------|-----------|
| TC-F-01 | `findOrCreateByPhone()` cria novo paciente no primeiro contato | P1 | unit |
| TC-F-02 | `findOrCreateByPhone()` registra `consentRecordedAt` no momento da criação | P1 | unit |
| TC-F-03 | `findOrCreateByPhone()` retorna paciente existente no segundo contato (idempotência) | P2 | unit |
| TC-F-04 | `findOrCreateByPhone()` usa o `professionalId` correto na busca | P1/P2 | unit |
| TC-F-05 | `updateProfile()` atualiza nome do paciente | P3 | unit |
| TC-F-06 | `updateProfile()` atualiza data de nascimento do paciente | P4 | unit |
| TC-F-07 | `updateProfile()` retorna PATIENT_NOT_FOUND para paciente inexistente | P6 | unit |
| TC-F-08 | `updateProfile()` retorna UNAUTHORIZED quando profissional não é o responsável | P5 | unit |
| TC-F-09 | `getById()` retorna paciente quando profissional é o responsável | P7 | unit |
| TC-F-10 | `getById()` retorna PATIENT_NOT_FOUND para paciente inexistente | P9 | unit |
| TC-F-11 | `getById()` retorna UNAUTHORIZED para paciente de outro profissional | P8 | unit |
| TC-F-12 | `anonymize()` substitui dados pessoais por nulos | P10 | unit |
| TC-F-13 | `anonymize()` substitui `phoneNumber` por hash irreversível (não o original) | P10 | unit |
| TC-F-14 | `anonymize()` retorna PATIENT_NOT_FOUND para paciente inexistente | P12 | unit |
| TC-F-15 | `anonymize()` retorna UNAUTHORIZED quando profissional não é o responsável | P11 | unit |
| TC-F-16 | `getHistory()` retorna histórico de interações do paciente | P13 | unit |
| TC-F-17 | `getHistory()` retorna UNAUTHORIZED para acesso de outro profissional | P14 | unit |

---

## 2. Confiabilidade
> Comportamento sob falha.

Relevante: SIM — idempotência de `findOrCreateByPhone()` é crítica para evitar duplicatas.

### Casos cobertos
- TC-F-03: chamada duplicada retorna o mesmo paciente (sem criar novo) — UNIQUE constraint como backstop no banco.

---

## 3. Performance
> Tempo de resposta.

Relevante: NÃO — o requisito de cadastro em segundo plano (sem impactar o tempo de resposta) é garantido pela arquitetura assíncrona do ConversationOrchestrator, não pelo PatientService isolado.

---

## 4. Segurança
> Controle de acesso, LGPD.

Relevante: SIM

### Casos de teste
| ID | Descrição | Vetor | Tipo Jest |
|----|-----------|-------|-----------|
| TC-S-01 (≡TC-F-08) | `updateProfile()` rejeita acesso de profissional diferente | acesso cruzado de dados | unit |
| TC-S-02 (≡TC-F-11) | `getById()` rejeita acesso de profissional diferente | acesso cruzado de dados | unit |
| TC-S-03 (≡TC-F-13) | `anonymize()` substitui phone por hash irreversível | LGPD — irreversibilidade | unit |
| TC-S-04 (≡TC-F-15) | `anonymize()` rejeita solicitação de profissional diferente | acesso cruzado LGPD | unit |

---

## 5. Manutenibilidade
> Separação de camadas, injeção de dependência.

Relevante: SIM

### Rubrica de testabilidade
| Dimensão | Pontuação (0–25) | Observação |
|---|---|---|
| Injeção de dependência | 25 | `IPatientRepository` injetável; clock injetável |
| Funções puras | 25 | `checkAccess()` é método puro (sem side effects além de leitura) |
| Separação de camadas | 25 | Não conhece Prisma; usa apenas interface de porta |
| Observabilidade | 25 | Todos os resultados são `Result<T>` testáveis |
| **Total** | **100/100** | |

---

## 6. Usabilidade
> Clareza das mensagens de erro.

Relevante: NÃO — códigos de erro são consumidos pelo controller, não diretamente pelo usuário.

---

## Cobertura de quality characteristics
| Característica | Relevante | Com teste |
|---|---|---|
| Funcionalidade | ✓ | ✓ |
| Confiabilidade | ✓ | ✓ (via TC-F-03) |
| Performance | N/A | — |
| Segurança | ✓ | ✓ |
| Manutenibilidade | ✓ | ✓ (estrutural) |
| Usabilidade | N/A | — |

**Cobertura = 3 ÷ 3 × 100% = 100%**

---

## Resultado da execução
| Métrica | Valor |
|---------|-------|
| Total de testes | 17 |
| Passaram | 17 |
| Falharam | 0 |
| Cobertura de linha | 97,36% |
| Cobertura de branch | 93,33% |
| pass@1 | ✅ sim |
