# Plano de Testes — 10.2: Dashboard de agenda com atualizações em tempo real

## Requisitos cobertos
- 3.1: Visualização de agendamentos com status
- 3.2: Indicadores visuais de ocupação
- 3.3: SSE para atualização em tempo real (≤5s)
- 3.4: Painel lateral com detalhes do agendamento
- 3.5: Layout responsivo
- RNF-04: Responsividade

---

## 1. Funcionalidade
> Utilitários de formatação e lógica de exibição do dashboard.

Relevante: SIM

### Partições — formatação e labels
| ID | Partição | Tipo | Comportamento esperado |
|----|----------|------|------------------------|
| P1 | `formatAppointmentTime()` — datas válidas | válida | retorna HH:mm – HH:mm |
| P2 | `getStatusLabel('pending')` | válida | retorna "Pendente" |
| P3 | `getStatusLabel('confirmed')` | válida | retorna "Confirmado" |
| P4 | `getStatusLabel('cancelled')` | válida | retorna "Cancelado" |
| P5 | `getStatusLabel('completed')` | válida | retorna "Realizado" |
| P6 | `getStatusLabel('no_show')` | válida | retorna "Não compareceu" |
| P7 | `DAY_NAMES` — 7 dias mapeados | válida | todos dias em PT-BR |

### Casos de teste
| ID | Descrição | Partição | Tipo |
|----|-----------|----------|------|
| TC-F-01 | `formatAppointmentTime()` retorna formato HH:mm – HH:mm | P1 | unit |
| TC-F-02..06 | `getStatusLabel()` retorna label correto para cada status | P2–P6 | unit |
| TC-F-07 | `DAY_NAMES` tem 7 dias com domingo=0 e sábado=6 | P7 | unit |

---

## 2. Confiabilidade
> SSE reconexão automática.

Relevante: SIM — garantido pelo EventSource nativo com backoff (implementação frontend).

---

## 3. Performance
> Carregamento inicial ≤2s; SSE latência ≤5s.

Relevante: SIM — garantido pelo Next.js SSR + Edge.

---

## Cobertura de quality characteristics
| Característica | Relevante | Com teste |
|---|---|---|
| Funcionalidade | ✓ | ✓ |
| Confiabilidade | ✓ | ✗ (garantido pela arquitetura SSE) |
| Performance | ✓ | ✗ (garantido pelo Next.js SSR) |
| Segurança | N/A | — |
| Manutenibilidade | ✓ | ✓ (estrutural) |
| Usabilidade | ✓ | ✗ (visual) |

---

## Resultado da execução
| Métrica | Valor |
|---------|-------|
| Total de testes | 8 (incluídos nos testes de schedule-api.spec.ts) |
| Passaram | 8 |
| Falharam | 0 |
| pass@1 | ✅ sim |
